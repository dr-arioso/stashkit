[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "stashkit"
version = "0.1.0"
description = "Resolver-based ontogeny and ontology toolkit"
requires-python = ">=3.9"
