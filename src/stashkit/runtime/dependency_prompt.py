"""
Stub for optional dependency installation UX.

This module intentionally does NOT perform installation yet.
"""

def prompt_install(dependencies: list[str]) -> bool:
    """
    Ask the user whether to install optional dependencies.

    Returns True if user agrees, False otherwise.
    """
    raise NotImplementedError(
        "Optional dependency installation not yet implemented"
    )
